#!/bin/bash

set -eu
source ~/.mutein_settings
export SGE_TASK_ID
conda activate ${MUT_PREFIX}trim-galore
TASKLIST=$1

$(vc_extract_params --params "threads accession" --tasklist ${TASKLIST})

echo fastqc -t ${threads} ${accession}/*.fastq.gz
